
```
curl https://github.com/gohugoio/hugo/releases/download/v0.79.1/hugo_extended_0.79.1_macOS-64bit.tar.gz -o hugo.tar.gz
tar xvf hugo.tar.gz
mv hugo ~/.local/bin

brew install mvn

curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh

cd .. && cargo build -p polar-c-api
gem install yard

alias pip="uv pip"
```
