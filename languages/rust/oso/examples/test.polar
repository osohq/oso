allow("sam", "get", resource: Blorg) if
    fib(10, res) and
    resource.id = res and
    resource.blog = "abc";



fib(1, 1);
fib(0, 1);
fib(n, res) if
    fib(n-1, x) and fib(n-2, y) and
    res = x + y
    and cut;